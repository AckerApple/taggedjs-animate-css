{
  "name": "taggedjs-animate-css",
  "type": "module",
  "private": true,
  "version": "0.0.17",
  "description": "HTML and CSS animations for TaggedJs brought to us by animate-css",
  "main": "js/index.js",
  "scripts": {
    "build:js": "rm -rf ./js && tsc -p tsconfig.build.json",
    "build": "npm-run-all --sequential build:js build:webpack build:dist:only build:gh-pages && echo \"✅ Build completed\"",
    "build:webpack": "npx webpack",
    "build:gh-pages": "cp -R js/. ../gh-pages/node_modules/taggedjs-animate-css/js && cd ../gh-pages && npm run build && echo \"🔬 Built gh-pages\"",
    "build:watch:gh-pages": "chokidar 'ts/**/*' -c 'npm run build:js && npm run build:dist:only && npm run build:gh-pages'",
    "build:dist": "run-s build:js build:webpack build:dist:only",
    "build:dist:only": "npm run copy:dist && npm run build:dist:pack && rm -rf ./dist/js && cp -R js ./dist && cp -R ./dist/. ../dist",
    "copy:dist": "cp animate.min.css ./dist/animate.min.css && cp README.MD ./dist/README.md",
    "test": "jest",
    "save:dist": "run-s patch:version build:js build:dist:only save:dist:git",
    "save:dist:git": "cd ../dist && git add . && git commit -m \"dist patch\" && git push",
    "save": "git add . && git commit -m \"patch\" && git push",
    "build:dist:pack": "cp package.json ./dist/package.json && echo \"✅ copied package.json to ./dist\" && node scripts/package-dist.cjs",
    "patch:version": "npm --no-git-tag-version version patch",
    "publish": "npm run build:dist && npm run save:dist && cd ../dist && npm publish",
    "build:watch": "chokidar 'ts/**/*' -c 'npm run build'"
  },
  "keywords": [
    "react",
    "angular",
    "vue",
    "svelte",
    "alternative",
    "works",
    "native",
    "in",
    "web",
    "browser"
  ],
  "author": "Acker Dawn Apple",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.23.6",
    "@babel/preset-env": "^7.23.6",
    "@types/jest": "^29.5.12",
    "chokidar-cli": "^3.0.0",
    "compression-webpack-plugin": "^11.1.0",
    "jest": "^29.7.0",
    "nodemon": "^3.0.2",
    "npm-run-all": "^4.1.5",
    "ts-jest": "^29.1.2",
    "ts-loader": "^9.5.1",
    "typescript": "^5.3.3",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  },
  "peerDependencies": {
    "taggedjs": "^2.8.38"
  }
}
